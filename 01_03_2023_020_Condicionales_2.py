#corrige el siguiente codigo
"""
color = rojo

else color == rojo
Print "El color es rojo."
if color != rojo
Print "El color no es rojo.
"""
#color debe ser un string
color = "rojo"
#primero debe ir el if antes que else
if color == 'rojo':
#la funcion print() esta mal
#el codigo esta mal indexado y else no lleva condicion y lleba :
    print("El color es rojo.")
else:
    print("El color no es rojo.")
#si cambiamos el string que guarda la variable color se ejecutara la secion de codigo del else